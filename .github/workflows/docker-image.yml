name: Build and Push Image (GHCR)

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: "要构建的版本标签（覆盖 release 标签）"
        required: false
        default: ""

concurrency:
  group: image-ghcr-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  packages: write

jobs:
  # 先跑质量门禁：测试不过就不产出镜像
  verify:
    uses: ./.github/workflows/verify.yml

  build-and-push:
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 只构建 linux/amd64，因此不需要 QEMU：
      # arm64 在 amd64 runner 上只能靠模拟（QEMU）执行，前端 tsc/vite 与 go build
      # 会被拖慢数倍到数十倍；且前端产物与架构无关，多平台时会被重复构建一遍。
      # 以后确实需要 arm64 时，应改用原生 arm64 runner，而不是在这里开模拟。

      - name: 配置 Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver: docker-container

      - name: 生成镜像标签与 OCI 标签
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/app-version-manage
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=ref,event=tag
            type=raw,value=${{ github.event.inputs.version }},enable=${{ github.event_name == 'workflow_dispatch' && github.event.inputs.version != '' }}
            type=raw,value=latest,enable=${{ github.event_name == 'workflow_dispatch' || (github.event_name == 'release' && !github.event.release.prerelease) }}

      - name: 登录 GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 构建并推送（linux/amd64）
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ steps.meta.outputs.version }}
            REVISION=${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: true
          sbom: true

      - name: 输出结果
        run: echo "${{ steps.meta.outputs.tags }}"
