name: CI

on:
  push:
    branches: ["**"]
  pull_request:

# 同一分支的旧构建自动取消，省 runner
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # 全部规则集中在可复用 workflow 中，与「发布镜像」时用的是同一套
  verify:
    uses: ./.github/workflows/verify.yml
